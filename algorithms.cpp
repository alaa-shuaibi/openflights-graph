#include <iostream>
#include <fstream>
#include <sstream>
#include <cstdlib>
#include <string>
#include <vector>
#include "algorithms.h"

using namespace std;

void Algorithms::insert(const string & filename){
    /*
    string line;
    ifstream myfile (filename);
    if (myfile.is_open() == true){
        while (getline(myfile, line) == true){
            Flights_Available.push_back(line);
        }
    }
    size_ = Flights_Available.size();
    */
}

int **Algorithms::createGrid(const string & filename){
    int** ret;
    return ret;
    /*string line;
    ifstream myfile (filename);
    if (myfile.is_open() == true){
        while (getline(myfile, line) == true){
            
        }
    }*/

    for (int idx = 0; idx < Flights_Available.size(); idx++){
        //doub
    }
}

int **Algorithms::Dijkstra(){

}